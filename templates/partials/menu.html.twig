<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">OPEN BAR</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
    {% set route_name = app.request.attributes.get('_route') %}
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link{% if route_name == "home" %} active {% endif %}" href="{{ path('home') }}">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if route_name == "beers" %} active {% endif %}" href="{{ path('beers') }}">Beers</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{% if route_name == "clients" %} active {% endif %}" href="{{ path('clients') }}">Clients</a>
            </li>
        </ul>
    </div>
</nav>